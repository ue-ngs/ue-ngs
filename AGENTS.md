# AIエージェント向け指示書 (Agents.md)

このプロジェクトは「UE長崎」の公式サイトプロジェクトです。AIエージェントが本プロジェクトを操作・開発する際は、以下のルールに従ってください。

## 開発環境と技術スタック
- 静的サイトジェネレーターとして **Pelican** を使用。
- Pythonのパッケージ・環境管理には **uv** を使用。
- フロントエンドの3Dシーンには **Three.js** (Vanilla JS) を使用。

## 開発・実行コマンド
プロジェクトのビルドやローカルサーバーの起動には、必ずルートディレクトリにある `serve.sh` を使用してください。

- **`./serve.sh setup`** : 初回セットアップ（Python仮想環境の構築と依存関係のインストール）
- **`./serve.sh build`** : Pelicanのビルドおよび `build.py` による `events.json` の生成
- **`./serve.sh serve [port]`** : ビルドの実行と、`output` ディレクトリをルートとしたHTTPサーバーの起動（デフォルトポート: 8000）
- **`./serve.sh setup-serve`** : セットアップ、ビルド、サーバー起動を一括実行

※ `serve.sh` 内部で仮想環境の有効化（`source .venv/bin/activate`）を行っているため、エージェントが自ら個別の環境有効化コマンドを実行する必要はありません。

## 特記事項
- `events.json` はトップページの「NEXT EVENT」表示に必須のファイルです。静的ファイル生成時は必ず `build.py` が実行されるように設計されています。
- イベント記事を追加・修正する場合は、`content/events/` にMarkdownファイルとして配置してください。フロントマターを正しく設定することで自動的にサイトへ反映されます。
